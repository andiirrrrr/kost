@extends('layouts.tenant', ['title' => 'Tagihan Saya'])
@section('content')
<div class="flex flex-col gap-6"><div><p class="text-sm font-semibold text-amber-700">Keuangan</p><h1 class="text-2xl font-bold">Tagihan Saya</h1></div>
<div class="flex gap-2 overflow-x-auto pb-1">@foreach(['' => 'Semua', 'unpaid' => 'Belum Bayar', 'pending' => 'Menunggu', 'paid' => 'Lunas', 'overdue' => 'Terlambat'] as $value => $label)<a href="{{ route('tenant.invoices.index', $value ? ['status' => $value] : []) }}" class="whitespace-nowrap rounded-full px-4 py-2 text-sm font-semibold {{ $filter === $value ? 'bg-slate-950 text-white' : 'border border-slate-200 bg-white text-slate-600' }}">{{ $label }}</a>@endforeach</div>
<div class="grid gap-3">@forelse($invoices as $invoice)<a href="{{ route('tenant.invoices.show', $invoice) }}" class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm transition hover:border-amber-300"><div class="flex items-start justify-between gap-3"><div><p class="font-bold">{{ $invoice->invoice_number }}</p><p class="mt-1 text-sm text-slate-500">Periode {{ sprintf('%02d/%d', $invoice->period_month, $invoice->period_year) }}</p></div><x-status-badge :status="$invoice->status" /></div><div class="mt-5 flex items-end justify-between"><div><p class="text-xs text-slate-500">Jatuh tempo</p><p class="text-sm font-semibold">{{ $invoice->due_date->locale('id')->translatedFormat('d F Y') }}</p></div><p class="text-lg font-bold">Rp {{ number_format($invoice->total_amount, 0, ',', '.') }}</p></div></a>@empty<div class="rounded-3xl border border-dashed border-slate-300 bg-white py-14 text-center"><p class="font-semibold">Belum ada tagihan untuk Anda.</p></div>@endforelse</div>{{ $invoices->links() }}</div>
@endsection
