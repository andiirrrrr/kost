@extends('layouts.tenant', ['title' => 'Riwayat Pembayaran'])
@section('content')
<div class="flex flex-col gap-6"><div><p class="text-sm font-semibold text-amber-700">Keuangan</p><h1 class="text-2xl font-bold">Riwayat Pembayaran</h1></div><div class="grid gap-3">@forelse($payments as $payment)<article class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div class="flex items-start justify-between gap-3"><div><p class="font-bold">{{ $payment->payment_number }}</p><p class="mt-1 text-sm text-slate-500">{{ $payment->invoice?->invoice_number }} · {{ sprintf('%02d/%d', $payment->invoice?->period_month, $payment->invoice?->period_year) }}</p></div><x-status-badge :status="$payment->status" /></div><div class="mt-4 flex items-end justify-between"><p class="text-sm text-slate-500">{{ $payment->paid_at?->locale('id')->translatedFormat('d M Y, H:i') }}</p><p class="text-lg font-bold">Rp {{ number_format($payment->amount, 0, ',', '.') }}</p></div>@if($payment->status === \App\Enums\PaymentStatus::REJECTED)<p class="mt-4 rounded-xl bg-red-50 px-4 py-3 text-sm text-red-800"><strong>Alasan:</strong> {{ $payment->rejection_reason }}</p>@endif@if($payment->proof)<a href="{{ route('tenant.payments.proof', $payment) }}" class="mt-4 inline-block text-sm font-semibold text-amber-700">Lihat bukti pembayaran</a>@endif</article>@empty<div class="rounded-3xl border border-dashed border-slate-300 bg-white py-14 text-center"><p class="font-semibold">Belum ada riwayat pembayaran.</p></div>@endforelse</div>{{ $payments->links() }}</div>
@endsection
