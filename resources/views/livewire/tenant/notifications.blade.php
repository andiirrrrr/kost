<div class="flex flex-col gap-6">
    <div class="flex items-start justify-between gap-4"><div><p class="text-sm font-semibold text-amber-700">Pembaruan</p><h1 class="text-2xl font-bold">Notifikasi</h1></div><button wire:click="markAllAsRead" class="text-sm font-semibold text-amber-700">Tandai semua dibaca</button></div>
    <div class="flex gap-2"><button wire:click="$set('filter', 'all')" class="rounded-full px-4 py-2 text-sm font-semibold {{ $filter === 'all' ? 'bg-slate-950 text-white' : 'border border-slate-200 bg-white' }}">Semua</button><button wire:click="$set('filter', 'unread')" class="rounded-full px-4 py-2 text-sm font-semibold {{ $filter === 'unread' ? 'bg-slate-950 text-white' : 'border border-slate-200 bg-white' }}">Belum dibaca</button></div>
    <div class="grid gap-3">@forelse($notifications as $notification)<article wire:key="notification-{{ $notification->id }}" class="rounded-2xl border p-5 shadow-sm {{ $notification->read_at ? 'border-slate-200 bg-white' : 'border-amber-200 bg-amber-50' }}"><div class="flex items-start justify-between gap-4"><div><h2 class="font-bold">{{ $notification->data['title'] ?? 'Notifikasi' }}</h2><p class="mt-1 text-sm leading-6 text-slate-600">{{ $notification->data['message'] ?? '' }}</p><p class="mt-2 text-xs text-slate-400">{{ $notification->created_at->locale('id')->diffForHumans() }}</p></div>@unless($notification->read_at)<button wire:click="markAsRead('{{ $notification->id }}')" class="whitespace-nowrap text-xs font-semibold text-amber-700">Tandai dibaca</button>@endunless</div>@if($notification->data['url'] ?? null)<a href="{{ $notification->data['url'] }}" class="mt-3 inline-block text-sm font-semibold text-amber-700">Buka detail →</a>@endif</article>@empty<div class="rounded-3xl border border-dashed border-slate-300 bg-white py-14 text-center"><p class="font-semibold">Semua sudah dibaca.</p></div>@endforelse</div>{{ $notifications->links() }}
</div>
